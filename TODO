Figure out when we need to add the + on gopher0 menus:

 * Always?  (So that servers linking in from gopher0 servers no
   that we speak gopher+)

 * Always except when linking to external servers?
   (So that clients don't try to use gopher0 right off the bat with them)
